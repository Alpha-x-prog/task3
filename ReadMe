# 📚 API Спецификация для панели администратора

### 🔍 Получить все товары
- **GET** `/api/products`
- 📄 **Ответ:** JSON-массив всех товаров

### ➕ Добавить товары
- **POST** `http://localhost:8080/api/products/api/products`
- 📦 **Тело запроса:**
```json
[
  { "name": "Товар", "price": 1000, "description": "Описание", "categories": ["Категория1"] }
]

### ✏️ Редактировать товар
PUT /api/products/{id}
📦 Тело запроса: поля для обновления
http://localhost:8080/api/products/1
{"price":14000}
🎯 Ответ: Обновлённый товар

###🗑 Удалить товар
DELETE /api/products/{id}
🎯 Ответ: 204 No Content





























🧪 Пример запросов (с помощью curl)
📩 Добавить товар:

bash
Копировать
Редактировать
curl -X POST -H "Content-Type: application/json" \
-d '[{"name":"Новый товар","price":15000,"description":"Описание нового товара","categories":["Электроника"]}]' \
http://localhost:8080/api/products
📝 Редактировать товар:

bash
Копировать
Редактировать
curl -X PUT -H "Content-Type: application/json" \
-d '{"price":14000}' \
http://localhost:8080/api/products/1
🗑 Удалить товар:

bash
Копировать
Редактировать
curl -X DELETE http://localhost:8080/api/products/1